/* Reset et variables */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

:root {
  --neon-green: #00ff66;
  --dark-bg: #0d0d0d;
  --rust-orange: #ff5500;
  --text-gray: #cccccc;
}

/* Corps de la page de garde */
body {
  font-family: 'Share Tech Mono', monospace;
  background-color: var(--dark-bg);
  color: var(--text-gray);
  height: 100vh;
  overflow: hidden;
}

/* Conteneur principal avec image post-apocalyptique */
.cover-container {
  position: relative;
  height: 100vh;
  width: 100%;
  background-image: url("https://images.unsplash.com/photo-1536152470836-b943b246224c?q=80&w=1920"); /* Paysage sombre / brumeux */
  background-size: cover;
  background-position: center;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 2rem;
}

/* Filtre sombre pour faire ressortir le texte */
.overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: radial-gradient(circle, rgba(0,0,0,0.6) 0%, rgba(13,13,13,0.95) 100%);
  z-index: 1;
}

/* Interface du Robot (Le cadre central) */
.scifi-interface {
  position: relative;
  z-index: 2;
  max-width: 800px;
  width: 100%;
  background-color: rgba(20, 20, 20, 0.85);
  padding: 3rem;
  border: 2px solid var(--neon-green);
  border-radius: 4px;
  box-shadow: 0 0 20px rgba(0, 255, 102, 0.2), inset 0 0 15px rgba(0, 255, 102, 0.1);
  text-align: center;
}

/* Éléments de l'interface */
.system-status {
  font-size: 0.9rem;
  color: var(--neon-green);
  letter-spacing: 2px;
  margin-bottom: 1.5rem;
  animation: blink 1.5s infinite;
}

/* Titre principal */
.glitch-title {
  font-family: 'Orbitron', sans-serif;
  font-size: 3rem;
  font-weight: 700;
  color: #ffffff;
  text-transform: uppercase;
  letter-spacing: 3px;
  line-height: 1.2;
  margin-bottom: 1rem;
  text-shadow: 0 0 10px rgba(255, 255, 255, 0.3);
}

.robot-name {
  color: var(--rust-orange);
  text-shadow: 0 0 10px rgba(255, 85, 0, 0.5);
}

.subtitle {
  font-size: 1.2rem;
  font-style: italic;
  color: #888;
  margin-bottom: 3rem;
}

/* Infos du livre */
.author {
  font-size: 1.3rem;
  color: #fff;
  letter-spacing: 1px;
  margin-bottom: 1.5rem;
}

.radiation-warning {
  display: inline-block;
  border: 1px dashed var(--rust-orange);
  padding: 0.5rem 1.5rem;
  color: var(--rust-orange);
  font-weight: bold;
  font-size: 0.9rem;
  background-color: rgba(255, 85, 0, 0.05);
  margin-bottom: 3rem;
}

/* Bouton d'action futuriste */
.btn-enter {
  display: inline-block;
  font-family: 'Orbitron', sans-serif;
  padding: 1rem 2.5rem;
  color: var(--dark-bg);
  background-color: var(--neon-green);
  text-decoration: none;
  font-weight: bold;
  letter-spacing: 2px;
  border-radius: 3px;
  transition: all 0.3s ease;
  box-shadow: 0 0 15px rgba(0, 255, 102, 0.4);
}

.btn-enter:hover {
  background-color: transparent;
  color: var(--neon-green);
  border: 1px solid var(--neon-green);
  box-shadow: 0 0 25px rgba(0, 255, 102, 0.8);
  transform: translateY(-2px);
}

/* Animations */
@keyframes blink {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.4; }
}

/* Responsive */
@media (max-width: 600px) {
  .scifi-interface {
    padding: 1.5rem;
  }
  
  .glitch-title {
    font-size: 2rem;
  }
  
  .subtitle {
    font-size: 1rem;
    margin-bottom: 2rem;
  }
  
  .radiation-warning {
    margin-bottom: 2rem;
  }
}
